# Strat examples

These standalone sources are small language examples, not registered trading
strategies. They are checked through the browser compiler so the examples and
the language reference cannot drift silently.

The conformance corpus lives under `strat/conformance/`. Canonical strategy
sources remain separate under `strategies/source/`.
